#include <stdio.h>
//if is a conditional statement that results to tru if statement is met

int main()
{
	int x=50, y=20; //assingning value to the variables
	if(x>y) //checking the condition
	{
		printf("x is greater than y\n");
	}
	return (0);
}
	
