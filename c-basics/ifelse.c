#include <stdio.h>
// if-else statements
// print a if the condition is met else print b

int main()
{
	int x=50, y=20;
	if(x==y)
	{
		printf("x is equal to y\n");
	}
	else
	{
		printf("x is not equal to y\nt");
	}
	return(0);
}
